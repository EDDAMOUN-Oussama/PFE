# Same-origin Vite proxy; checkout location is detected automatically.
# BACKEND_ORIGIN=http://localhost
# BACKEND_PATH=/pfe/PFE/backend/controllers
# For production configure a same-origin /api reverse proxy, or set an absolute URL:
# VITE_API_BASE_URL=https://example.com/backend/controllers
